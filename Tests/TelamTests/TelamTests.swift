    import XCTest
    @testable import Telam

    final class TelamTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(Telam().text, "Hello, World!")
        }
    }
